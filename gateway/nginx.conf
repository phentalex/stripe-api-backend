server {
    listen 80;
    server_tokens off;
    index index.html;

    location / {
        proxy_set_header Host $http_host;
        proxy_pass http://backend:8000/;
        
        alias /static/;
    }
}